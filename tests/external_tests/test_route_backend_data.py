invalid_token = {
    "errors": [
        "Auth token is invalid"
    ]
}

expired_token = {
    "errors": [
        "Auth token has expired"
    ]
}

non_decodeable_token = {
    "errors": [

        "Invalid auth token"
    ]
}
